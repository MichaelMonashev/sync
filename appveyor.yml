version: "{build}"

clone_folder: c:\gopath\src\github.com\MichaelMonashev\sync

environment:
 GOPATH: c:\gopath

install:
- echo %PATH%
- echo %GOPATH%
- go version
- go env
- go get -v -t ./...

build_script:
- go build -v ./netmutex
- go test -v ./netmutex
- go vet ./netmutex
